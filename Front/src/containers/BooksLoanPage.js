import React, { Component } from 'react';

class BooksLoanPage extends Component {

  render() {
    return (
      <div>{'AUTORES'}</div>
    );
  }
}

export default BooksLoanPage;//Conectarlo con redux para recibir props. Los necesarios para motrar las 3 cajas
//Peticion auto de generos autores y libros al cargar la página (component will mount)
